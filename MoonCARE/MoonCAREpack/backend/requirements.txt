fastapi==0.109.0
uvicorn[standard]==0.27.0
sqlalchemy>=2.0.30
psycopg2-binary==2.9.9
pydantic[email]==2.13.3
pydantic-settings==2.1.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
websockets==12.0
httpx==0.26.0
numpy==1.26.3
python-dotenv>=1.0.0

openai>=1.0.0

# Redis Stack for semantic caching
redis>=5.0.0

# HTTP/2 support
h2>=4.1.0

# Prometheus metrics
prometheus-client>=0.20.0

# Token counting
tiktoken>=0.5.2

alembic>=1.13.0

# Optional local inference / embedding dependencies are intentionally not
# installed in the Docker handoff image because they make server builds heavy.
# Install them manually only on a dedicated inference machine if needed:
# sentence-transformers>=2.2.0
# vllm>=0.6.0
