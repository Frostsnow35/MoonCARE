# 开发模式空输入登录功能

## 功能说明

在开发环境（DEBUG模式）下，支持通过**空输入**直接登录系统，方便快速测试功能，无需每次都注册账号或输入验证码。

## 使用方法

### 前提条件

确保环境变量 `DEBUG=true`（默认在开发环境中已启用）

### 操作步骤

1. 打开登录页面
2. **邮箱和密码字段都留空**（不输入任何内容）
3. 点击"登录"按钮
4. 系统会自动创建测试用户并完成登录

## 测试账号信息

| 字段 | 值 |
|------|-----|
| 邮箱 | `test@mooncare.local` |
| 密码 | `test123456` |
| 昵称 | `测试用户` |

## 技术实现

### 后端逻辑

- **触发条件**：`DEBUG=true` 且 `email` 和 `password` 都为空
- **自动创建**：首次使用时自动创建测试用户
- **复用账号**：后续空输入登录会复用已创建的测试账号
- **安全限制**：仅在 `DEBUG=true` 时生效，生产环境无法使用

### 代码位置

[backend/app/api/v1/auth.py](file:///e:\MOONCARE\backend\app\api\v1\auth.py#L367-L394)

## 安全注意事项

⚠️ **重要**：此功能仅在开发环境可用

- 生产环境必须设置 `DEBUG=false`
- 生产环境无法通过空输入登录
- 测试账号仅用于本地开发，不会在生产环境创建

## 适用场景

- 快速测试前端登录流程
- 调试需要登录状态的功能
- 演示系统功能（开发环境）
- 避免每次测试都需要注册新账号

## 与正常登录的区别

| 特性 | 空输入登录 | 正常登录 |
|------|-----------|---------|
| 邮箱 | 空 | 必须填写有效邮箱 |
| 密码 | 空 | 必须填写正确密码 |
| 环境限制 | 仅DEBUG模式 | 所有环境 |
| 账号创建 | 自动创建 | 需要注册 |
| 邮箱验证 | 自动验证 | 需要验证码 |

## 故障排查

### 问题：空输入登录失败

**可能原因**：
1. `DEBUG=false`：检查环境变量配置
2. 数据库连接失败：检查数据库是否正常运行
3. 前端未发送空值：检查前端表单提交逻辑

**解决方法**：
```bash
# 检查DEBUG配置
grep DEBUG .env

# 确保值为true
DEBUG=true
```

## 相关配置

环境变量配置示例：

```ini
# 开发环境
DEBUG=true
DATABASE_URL=sqlite:///./healthai.db

# 生产环境（必须关闭）
DEBUG=false
DATABASE_URL=postgresql://user:password@host:5432/mooncare
```

## 更新日志

- **2026-05-21**：新增空输入登录功能，支持开发环境快速测试