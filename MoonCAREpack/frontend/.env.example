# Frontend API config.
# Keep /api/v1 for Docker one-image deployment and Vite proxy development.
VITE_API_BASE_URL=/api/v1

# Optional explicit websocket origin, for example: ws://localhost:8000
VITE_WS_BASE_URL=
